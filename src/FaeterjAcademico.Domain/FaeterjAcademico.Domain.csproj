<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Permite que os testes exercitem SolutionConstraints/SolutionCost/BuscaLocal
         diretamente (invariantes do motor GRASP), sem expor essas peças como API pública. -->
    <InternalsVisibleTo Include="FaeterjAcademico.Domain.Tests" />
  </ItemGroup>

</Project>
